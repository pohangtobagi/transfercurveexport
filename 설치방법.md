# 설치 순서

## 1. Supabase 만들기
1. https://supabase.com 에서 새 Project를 만듭니다.
2. 왼쪽 **SQL Editor** → **New query**를 누릅니다.
3. 이 폴더의 `supabase_setup.sql` 내용을 전부 붙여 넣고 **Run**을 누릅니다.

## 2. 키 복사
Supabase 왼쪽 **Project Settings → API**에서 다음 두 값을 복사합니다.
- Project URL
- `service_role` key

`service_role` key는 GitHub 코드에 절대 넣지 말고 Streamlit Secrets에만 넣습니다.

## 3. Streamlit Secrets 입력
Streamlit Cloud에서 앱을 열고:
**Manage app → Settings → Secrets**

아래 형식으로 입력합니다.

```toml
SUPABASE_URL = "여기에 Project URL"
SUPABASE_SERVICE_ROLE_KEY = "여기에 service_role key"
```

저장한 뒤 앱을 Reboot합니다.

## 4. GitHub에 넣을 파일
현재 GitHub repository의 기존 파일을 다음 파일로 교체합니다.
- 기존 `app.py` → 이 폴더의 `app.py`
- 기존 `requirements.txt` → 이 폴더의 `requirements.txt`

`supabase_setup.sql`은 GitHub에 올려도 되지만 앱 실행에는 필요하지 않습니다.

## 동작
프로젝트, Device Information, 로그, 슬라이더 상태, 업로드된 Excel 원본이 모두 `fet_app_state` 표의 한 행에 영구 저장됩니다. Streamlit 서버가 재시작되거나 재배포되어도 다시 불러옵니다.
